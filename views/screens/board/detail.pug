extends ../../layouts/base.pug

block content
    section.wholeWrapper
        section.XX__left
        section.rsWrapper 
            h1 #{bData.title}
            h3 #{bData.name}
            h4 #{bData.createdAt}
            h4 #{bData.content}
        section.XX__right
